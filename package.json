{
  "name": "ascendimacy-sts",
  "version": "0.1.0",
  "description": "LAST-CO runtime — MVP-01a walking skeleton",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "mvp01a": "node src/cli.js",
    "mvp01a:validate": "node src/cli.js --validate"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@modelcontextprotocol/sdk": "^1.10.2",
    "js-yaml": "^4.1.0",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "vitest": "^3.1.2"
  }
}
